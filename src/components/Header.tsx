
import { AppBar, Toolbar } from '@mui/material';

const Header: React.FC = ()  => {

    return(
        <AppBar>
            <Toolbar>
                Todo 
            </Toolbar>
        </AppBar>
    );

};
export default Header;
